import { View, Text } from 'react-native';
import { Container } from './styles.<%= props.extension %>';

export function <%= props.name %>() {
  return (
    <Container>
      <Text><%= props.name %></Text>
    </Container>
  );
};
