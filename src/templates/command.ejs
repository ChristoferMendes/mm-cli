import { Command } from 'gluegun/build/types/domain/command'
import { Toolbox } from '../@types/gluegun'

module.exports = {
  name: '<%= props.name %>',
  description: '<%= props.desc %>',
  alias: '<%= props.alias %>',
  run: async (toolbox: Toolbox) => {
    const { print } = toolbox

    return print.success('Command created!')
  },
} as Command
